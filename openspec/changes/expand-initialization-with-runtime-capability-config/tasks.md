## 1. Backend
- [x] 1.1 Finalize runtime capability schemas, grouped config persistence, encrypted secret storage, and validation APIs
- [x] 1.2 Route storage, document parsing, knowledge graph, and automation runtime consumers through the new resolver layer
- [x] 1.3 Add clear “not configured” capability errors and runtime fallback semantics for attachments and knowledge graph
- [x] 1.4 Extend initialization defaults and initialization submission so optional runtime modules can be applied atomically

## 2. Frontend
- [x] 2.1 Expand the initialization wizard into core steps plus a capability center with persisted drafts
- [x] 2.2 Build reusable runtime capability module forms and a new `/settings/system-setup` page
- [x] 2.3 Add friendly missing-state UX for graph and attachment flows when capabilities are skipped or unavailable
- [x] 2.4 Add i18n copy and layout polish for capability center and system setup surfaces

## 3. Validation
- [x] 3.1 Add backend tests for runtime config resolution, initialization submission with optional modules, and capability error behavior
- [x] 3.2 Run backend tests, frontend build, and OpenSpec validation
